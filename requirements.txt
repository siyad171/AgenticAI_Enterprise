# === Core (Required) ===
groq==0.33.0
python-dotenv==1.1.1

# === Web UI (Required) ===
streamlit==1.50.0
streamlit-ace==0.1.1
streamlit-webrtc>=0.47.0

# === PDF Parsing (Required for resume upload) ===
PyPDF2==3.0.1

# === HTTP Requests (Required for Judge0 code execution) ===
requests==2.31.0

# === Video Interview (Required for WebRTC video) ===
numpy>=1.24.0
opencv-python>=4.8.0

# === Testing ===
pytest>=7.0
pytest-cov>=4.0

# === Emotion Analysis (Optional — install for facial analysis) ===
# pip install deepface
# deepface==0.0.89
